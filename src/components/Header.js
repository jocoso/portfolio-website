import React from 'react';
import Navbar from './Navbar';

const Header = () => {
    return(
        <section id="header-section">
            <Navbar />
        </section>
    );
}

export default Header;